# Test-only dependencies. Install:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

testcontainers[postgresql]==4.8.1
pytest-cov==5.0.0
cryptography>=42.0.0   # для генерации test RSA-пары JWT в фикстуре
