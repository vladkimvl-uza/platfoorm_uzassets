[pytest]
testpaths = tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
markers =
    unit: pure-function tests, no DB/network
    integration: testcontainers-based tests (requires Docker)
